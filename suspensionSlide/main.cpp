#include "suspensionSlideTest.h"
#include <QtWidgets/QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    suspensionSlideTest w;
    w.show();
    return a.exec();
}
